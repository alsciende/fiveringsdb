<img src="{{ asset('images/pack_cards/' ~ packCard.pack.id ~ '/' ~ packCard.card.id ~ '.webp') }}"
     class="img-fluid rounded-4"
     width="350px" height="500px"
     alt="{{ packCard.card.name }}">
