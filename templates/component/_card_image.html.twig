<img src="{{ asset('images/card_image/' ~ packCard.pack.id ~ '/' ~ packCard.card.id ~ '.webp') }}"
     class="img-fluid rounded-4"
     width="302px" height="422px"
     alt="{{ packCard.card.name }}">
